import { StyleSheet } from "react-native";

export default StyleSheet.create({
  titleField: {
    marginTop: 20,
    fontSize: 20,
    fontWeight: "bold",
  },
  textInputField: {
    height: 50,
    fontSize: 18,
  },
});
